## 1. Основа репозитория

- [x] 1.1 Создать согласованную структуру каталогов, корневые служебные файлы и проектный `AGENTS.md`; проверить дерево файлов и отсутствие зависимости от `common`.
- [x] 1.2 Настроить `backend/pyproject.toml` для Python 3.13, `uv`, FastAPI, Advanced Alchemy, `asyncpg`, Alembic, Ruff, Pyright, pytest и coverage; проверить разрешение зависимостей и импорт установленного пакета `piecewise_linear`.
- [x] 1.3 Создать React/TypeScript/Vite frontend, добавить Recharts и `dnd-kit`, зафиксировать npm-зависимости; проверить установку зависимостей и production-сборку.
- [x] 1.4 Записать одобренные архитектурные решения в русскоязычные ADR проекта; проверить их согласованность с `design.md`.

## 2. Асинхронный backend

- [x] 2.1 Реализовать конфигурацию приложения, асинхронный движок и сессии Advanced Alchemy; проверить запуск приложения и корректное освобождение соединений.
- [x] 2.2 Создать модель точки с полями `id`, `x`, `y` и первую Alembic-миграцию; проверить применение и откат миграции на PostgreSQL.
- [x] 2.3 Реализовать асинхронные операции загрузки, добавления, редактирования и удаления точек; проверить их вручную через OpenAPI.
- [x] 2.4 Реализовать транзакционное переупорядочивание с блокировкой, проверкой полной перестановки, удалением и повторной записью всего набора точек с ID `1…N`; проверить успешную перестановку и полный откат ошибочной операции.
- [x] 2.5 Реализовать API `/api/v1/points`, схемы валидации и согласованные ответы `404`, `409` и `422`; проверить контракты в `/docs` и `/openapi.json`.
- [x] 2.6 Реализовать `/api/v1/health/live` без обращения к БД и `/api/v1/health/ready` с асинхронной проверкой PostgreSQL; проверить ответы при доступной и недоступной базе.
- [x] 2.7 Выполнить Ruff и Pyright для основного backend-кода и устранить замечания.

## 3. Одностраничный frontend

- [x] 3.1 Реализовать типизированный API-клиент и модель подтверждённого, локального и ошибочного состояния; проверить загрузку списка и восстановление состояния после имитированной ошибки запроса.
- [x] 3.2 Реализовать список точек, форму добавления, автоматическое сохранение координат и подтверждаемое удаление; проверить полный сценарий вручную в браузере.
- [x] 3.3 Реализовать drag-and-drop через `dnd-kit` без кнопок перемещения; проверить перетаскивание мышью, касанием и клавиатурой и применение серверного порядка.
- [x] 3.4 Реализовать график через Recharts с числовыми осями, сеткой, точками и прямыми соединяющими отрезками в порядке ID; проверить пустой список, одну точку, отрицательные и совпадающие координаты.
- [x] 3.5 Реализовать адаптивную компоновку списка и графика, состояния загрузки и сообщения об ошибках; проверить широкий и узкий экран без горизонтальной прокрутки страницы.
- [x] 3.6 Выполнить frontend lint, проверку типов и production-сборку основного кода и устранить замечания.

## 4. Контейнеры и эксплуатация

- [x] 4.1 Создать многоэтапный backend Dockerfile, который собирает приложение через `uv`, но не содержит `uv` в runtime-образе; проверить сборку и импорт установленного пакета внутри образа.
- [x] 4.2 Создать локальный frontend-контейнер и production-образ Nginx с собранной статикой; проверить раздачу страницы и проксирование `/api`, `/docs` и `/openapi.json`.
- [x] 4.3 Создать `compose.yaml` с PostgreSQL, миграцией, backend и Vite frontend; проверить локальный запуск полного сервиса одной командой.
- [x] 4.4 Создать `compose.prod.yaml` с PostgreSQL, миграцией, backend и Nginx без публикации БД наружу; проверить production-запуск и healthcheck контейнеров.
- [x] 4.5 Добавить `.env.example`, Makefile с командами установки, запуска, проверок, миграций, сборки и развёртывания и `.pre-commit-config.yaml`; проверить справку Makefile и отсутствие секретов в репозитории.
- [x] 4.6 Обновить русскоязычный README инструкциями локального запуска, API и Linux-развёртывания; проверить команды по чистому клону без каталога `common`.

## 5. Согласование основного кода

- [x] 5.1 Показать пользователю реализованный backend, frontend, миграции и контейнерную инфраструктуру; продолжить к созданию тестов только после явного одобрения основного кода.

## 6. Тесты после одобрения основного кода

- [x] 6.1 Создать unit-тесты backend для валидации и сервисной логики; проверить успешный запуск pytest и отчёт coverage.
- [x] 6.2 Создать интеграционные тесты backend с настоящим PostgreSQL для CRUD, конкурентных изменений, перенумерации и отката транзакции; проверить изолированный запуск интеграционного набора.
- [x] 6.3 Создать frontend-тесты ключевых состояний списка и графика; проверить их успешный запуск.
- [x] 6.4 Создать Playwright E2E полного пользовательского сценария добавления, редактирования, drag-and-drop и удаления точки; проверить сценарий против Compose-среды.
- [x] 6.5 Добавить проверки Docker-образов и обоих health endpoints при доступной и недоступной БД; проверить воспроизводимый запуск локально.
- [x] 6.6 Настроить pre-commit и Linux/WSL-команды репозитория для статических проверок, тестов, coverage, сборок и контейнерных проверок; выполнить полный набор проверок без ошибок.

## 7. Репозиторий и завершение

- [x] 7.1 Создать через GitHub API приватный репозиторий `PeTRUEha/piecewise-linear` и настроить `origin`; проверить владельца, имя, приватность и адрес удалённого репозитория.
- [x] 7.2 Проверить отсутствие секретов и зависимости от `common`, актуальность документации и состояние всех проверок; предоставить итоговую сводку, не создавая коммит без прямого указания пользователя.
